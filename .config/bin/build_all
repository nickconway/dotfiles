#!/usr/bin/env bash

if [[ $# -ne 1 ]]; then
    for d in $(find $PROJECT_DIR -maxdepth 1 -type d); do
        cd "$d"/Debug && make clean && make all
    done
else
    for d in $(find $1 -maxdepth 1 -type d); do
        cd "$d"/Debug && make clean && make all
    done
fi
