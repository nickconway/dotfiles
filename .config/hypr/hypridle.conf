general {
    lock_cmd = hyprlock
    before_sleep_cmd = loginctl lock-session; sleep 3
}

listener {
    timeout = 300
    on-timeout = loginctl lock-session
}

listener {
    timeout = 600
    on-timeout = systemctl suspend
}
