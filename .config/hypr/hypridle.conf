general {
    lock_cmd = hyprlock
    before_sleep_cmd = media -a pause; loginctl lock-session; sleep 1
}

listener {
    timeout = 300
    on-timeout = pidof hyprlock && systemctl sleep || loginctl lock-session
}

listener {
    timeout = 600
    on-timeout = systemctl sleep
}
