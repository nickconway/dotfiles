session_name: koui-dev
start_directory: "$KOTEST"
windows:
  - layout: main-horizontal
    options:
      main-pane-height: "50%"
    panes:
      - shell_command:
          - $CRYPTSOFT/kmipc_server/bin/kmip_server
      - shell_command:
          - cmd: NODE_ENV=development
          - cmd: sleep 3
          - cmd: node $KODIR/ko_rest/server-ui.js
