session_name: ${TMUXP_SESSION_NAME}
start_directory: ${TMUXP_START_DIR}
windows:
  - panes:
      - nvim
  - layout: main-horizontal
    options:
      main-pane-height: 60%
    panes:
      - pnpm run dev
      - docker compose up
