#!/usr/bin/env bash
set -euo pipefail

if [[ -n "${DEBUG_MODE:-}" ]]; then
	set -x
fi

command -v pass &>/dev/null

if command -v termux-reload-settings >/dev/null; then
    pass ansible/"$TERMUX_USER"
else
    pass ansible/"$USER"
fi
