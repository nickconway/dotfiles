#!/usr/bin/env bash
set -euo pipefail

command -v pass &>/dev/null

if command -v termux-reload-settings > /dev/null; then
    pass python-keyring/ansible/nick
else
    pass python-keyring/ansible/"$USER"
fi
