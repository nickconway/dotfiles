#!/bin/bash

set -xe

cd ~
ntfy publish -u "nick:$NTFY_PASSWORD" notifications "Backing up docker containers"
stop-containers
sudo tar czvf docker.tar.gz docker
start-containers
until ntfy publish -u "nick:$NTFY_PASSWORD" notifications "Backing up complete" > /dev/null; do
    true
done
