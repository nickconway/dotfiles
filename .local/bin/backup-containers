#!/bin/bash

set -xe

export PATH=$HOME/.local/bin:$PATH
. $HOME/.config/shell/common/encrypted.sh
DATE=$(date +"%d-%b-%Y-%H-%M-%S")

cd ~
curl -d "Backing up docker containers" https://ntfy.conway.dev/notifications -H "Authorization: Bearer ${NTFY_TOKEN}" || echo "Failed to notify of backup start"
echo "Stopping containers"
stop-containers
sudo tar czfv docker-$(hostname)-"$DATE".tar.gz docker $(sudo test -e /var/lib/docker/volumes && echo /var/lib/docker/volumes) $([[ -e ~/.local/share/docker/volumes ]] && echo .local/share/docker/volumes)
sudo chown $(whoami):$(whoami) docker-$(hostname)-"$DATE".tar.gz
start-containers

mv docker-$(hostname)-"$DATE".tar.gz ~/Dropbox/Documents/Backups/
cd ~/Dropbox
dropbox exclude add Documents/Backups/docker-$(hostname)-"$DATE".tar.gz

until [[ $(curl -f -s -d "Backing up complete" https://ntfy.conway.dev/notifications -H "Authorization: Bearer ${NTFY_TOKEN}") ]]; do
    sleep 3
done
