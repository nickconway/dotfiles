#!/usr/bin/env bash
set -euo pipefail

find ~/.config/quickshell -mindepth 1 -maxdepth 1 -type d -exec basename {} \; | fzft --border-label " Choose QuickShell Config " > ~/.config/quickshell/config
killall qs
qs -c $(cat ~/.config/quickshell/config) -d
