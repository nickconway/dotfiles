#!/bin/bash
if [[ -n "$DISPLAY" ]] && command -v rofi &> /dev/null; then
    cliphist list | rofi -dmenu -display-columns 2 -p Clipboard -i | cliphist decode | wl-copy && paste-keys
else
    cliphist list | fzf-tmux -p -- -d $'\t' --with-nth 2 | cliphist decode
fi
