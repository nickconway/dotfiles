#!/bin/bash

mkdir -p ~/Git
cd ~/Git
for r in $(gh repo list | awk '{print $1}' | sed "/.*dotfiles.*/d"); do
    REPO_NAME="$(echo $r | cut -d/ -f2)"
    [[ -e ~/Git/$REPO_NAME ]] && (cd $REPO_NAME; pwd; git pull) || gh repo clone $r
    cd ~/Git
done
