#!/usr/bin/env bash
set -euo pipefail

CWD="${PWD}"

while [ "${CWD}" != "" ] && [ "${CWD}" != '.' ] && [ ! -e "${CWD}/${1-}" ]; do
    CWD=${CWD%/*}
done

if [ -e "${CWD}/${1-}" ]; then
    echo "${CWD}/${1-}"
fi
