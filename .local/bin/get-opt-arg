#!/usr/bin/env bash
set -euo pipefail

OPTIND=$1
shift

TOKEN=${!OPTIND}

if [[ -n $TOKEN && $TOKEN != -* ]]; then
    OPTIND=$((OPTIND + 1))
    OPTARG=$TOKEN
else
    OPTARG=""
fi

echo "OPTIND='$OPTIND'"
echo "OPTARG='$OPTARG'"
