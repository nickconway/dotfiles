#!/usr/bin/env bash

if [[ $# -eq 0 ]]; then
    DIR=$PROJECT_DIR
else
    DIR=$1
fi

for d in $(find "$DIR" -maxdepth 2 -name ".git"); do
    cd $(dirname $d)
    echo $(basename "$PWD")
    git pull
    echo
done
