#!/usr/bin/env bash
set -euo pipefail

pgrep -af "$*" | grep -v $PPID | grep -v "$0 $*" | while read -r LINE; do
    kill $(cut -d ' ' -f 1 <<<"$LINE")
    eval "$(cut -d ' ' -f 2- <<<"$LINE")" &
done
