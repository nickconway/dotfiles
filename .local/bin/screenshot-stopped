#!/usr/bin/env bash
set -euo pipefail

if [[ -n "${DEBUG_MODE:-}" ]]; then
	set -x
fi

eval "$(notify-send "Screen Recording" "Screen Recording Saved to $1" -e --action "xdg-open '$1'=Open" -e)" &
pactl unload-module module-loopback
pactl unload-module module-null-sink
