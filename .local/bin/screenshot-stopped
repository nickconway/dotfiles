#!/usr/bin/env bash
set -euo pipefail

(
    ACTION=$(notify-send "Screen Recording" "Screen Recording Saved to $1" -e --action "open=Open" -e)
    case $ACTION in
    'open') xdg-open "$1" ;;
    esac
) &

pactl unload-module module-loopback
pactl unload-module module-null-sink
