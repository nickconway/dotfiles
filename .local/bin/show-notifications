#!/usr/bin/env bash
set -euo pipefail

if pgrep -afx "qs -c caelestia.*" &>/dev/null; then
    qs -c caelestia ipc call drawers toggle sidebar
elif pgrep -afx "qs -c noctalia.*" &>/dev/null; then
    qs -c noctalia ipc call notifications toggleHistory
else
    swaync-client -t
fi
