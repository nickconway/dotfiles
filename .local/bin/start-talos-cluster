#!/usr/bin/env bash
set -euo pipefail

if [[ -n "${LOG:-}" ]]; then
    exec &>"/tmp/$(basename "$0").log"
    set -x
fi

if [[ -n "${DEBUG_MODE:-}" ]]; then
    set -x
fi

IDS=(300 301 302 350 351 352)

for ID in "${IDS[@]}"; do
    echo "Starting ${ID}..."
    ssh pve sudo qm start ${ID}
done
