#!/usr/bin/env bash

if [[ $# -eq 1 ]]; then
    selected=$1
else
    selected=$(find ~/.config/tmuxp -mindepth 1 -maxdepth 1 | fzf --preview 'cat {}')
fi

if [[ -z $selected ]]; then
    exit 0
fi

tmuxp load $selected
