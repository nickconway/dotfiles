#!/usr/bin/env bash
set -euo pipefail

if [[ -n "${LOG:-}" ]]; then
    exec &>"/tmp/$(basename "$0").log"
    set -x
fi

if [[ -n "${DEBUG_MODE:-}" ]]; then
    set -x
fi

LOCK=/tmp/expand_toolbar.lock

if [ -f "$LOCK" ]; then
    exit 0
else
    exit 1
fi
